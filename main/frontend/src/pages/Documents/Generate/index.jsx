import React from 'react'
import DocumentAgent from '../Agent'

// Document Generate page now uses the same Agent UI with button-based interactions
export default function DocumentGenerate() {
  return <DocumentAgent />
}
